[project]
name = "first"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "biopython>=1.85",
    "chembl-webresource-client>=0.10.9",
    "pandas>=2.3.2",
]
